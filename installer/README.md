# installer — archiso Custom Profile

Custom archiso profile for bootable Holmium installer ISO (max 16GB). In-TUI first-run wizard (no whiptail/dialog). No Calamares.

- `profile/` — archiso profile directory
- `build.sh` — ISO build script
- `wizard/` — first-run TUI wizard
