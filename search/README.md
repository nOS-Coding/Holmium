# search — DuckDuckGo + SearXNG Fallback

DuckDuckGo primary search via `duckduckgo_search` library. SearXNG (running on Pi 5) as fallback. No Tavily.

- `search.py` — search interface
- `ddg.py` — DuckDuckGo wrapper
- `searxng.py` — SearXNG client (stubbed until Pi is on network)
