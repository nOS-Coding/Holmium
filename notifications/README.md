# notifications — ntfy.sh Push + Mac Bridge

Self-hosted ntfy.sh push notifications via WebSocket. Mac notification bridge for forwarding to macOS CLI client.

- `ntfy.py` — ntfy publish/subscribe wrapper
- `mac_bridge.py` — macOS notification bridge
