# holmium-cmd — macOS CLI + Daemon

Full Textual TUI for macOS (`holmium` command) over WireGuard HTTPS. Background daemon for notifications, clipboard, and image receiving.

- `tui.py` — macOS Textual TUI
- `daemon.py` — background notification/clipboard daemon
- `install.sh` — macOS install script
