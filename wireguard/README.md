# wireguard — WireGuard Server + netsh

WireGuard server config (10.0.0.1/24), key generation, and `netsh` CLI wrapper for managing network interfaces.

- `wg.conf` — server config
- `keys/` — key storage
- `netsh.py` — custom network CLI
- `client-configs/` — client .conf templates
