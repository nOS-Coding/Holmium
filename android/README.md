# android — Kotlin Android App

`com.holmium`. API 29+ (minSdk 29). Jetpack Compose UI, ntfy WebSocket push, PTT-only input, WireGuard TCP image listener, share sheet integration, dark theme default.

- `app/` — Android app module
- `gradle/` — build config
